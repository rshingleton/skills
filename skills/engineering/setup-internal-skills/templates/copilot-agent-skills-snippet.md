## Agent skills (internal)

Follow [AGENTS.md](../AGENTS.md) for project conventions. Runtime config: `docs/agents/` (issue tracker, triage labels, domain docs).

Intake: `docs/issues/` (`/issue-it`). Plan: `/plan-it` (evaluate + grill + `--from-issues`). Doc Cycle: `/plan-it` → `/implement-it` → `/audit-it` → `/verify-it` → `/commit-it`. Jira: `/plan-it --jira` → `jira.md`.
