## Agent skills (internal)

Follow [AGENTS.md](../AGENTS.md) for project conventions. Runtime config: `docs/agents/` (issue tracker, triage labels, domain docs).

Intake: `docs/issues/` (`/issues-it`, `/triage`). Plan: `/plan-it` (grill + `--from-issues`). Doc Cycle: `/plan-it` → `/implement-it` → `/audit-it` → `/verify-it`. Jira: `/plan-it --jira` → `jira.md`.
